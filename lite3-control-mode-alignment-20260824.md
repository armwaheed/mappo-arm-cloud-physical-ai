# Lite3 控制模式与自动控制路径对齐

**日期：** 2026-08-24
**目的：** 向内部研发团队说明“机器人可以站立但不能前进”的已验证原因、已经成功的
厂商高层控制路径，以及它与本项目最终自动控制目标的关系。

## 结论摘要

1. 本次解决的是**当前 Lite3 firmware 上的高层运动接口/控制模式不匹配**，不是通过
   修改关节参数、ONNX policy、MotionSDK 或 ROS 2 实现的。
2. 已在一次受限、单独授权的实体试验中证明：厂商的
   **手动控制模式 -> 移动模式 -> 心跳 + 简单前进轴指令** 可以让机器人前进并稳定停止。
3. 项目最终仍应由感知、规划和 MAPPO 自动决定运动意图；但这不必等于厂商 firmware
   标签中的“自主模式（Autonomous Mode）”。
4. 当前已证明的是“外部程序可作为虚拟摇杆驱动厂商板载步态控制器”的执行通道，
   **不是**“视觉导航/MAPPO 已获准实体运行”。

## 问题背景：为什么以前能站但不能走

此前验证过两条不同路径：

| 路径 | 已知现象 | 结论 |
| --- | --- | --- |
| 低层 RL / 12 关节控制路径 | 能完成站立流程；历史的前进输入没有得到可验证的实体前进 | 该路径仍缺少真实 payload、关节响应和物理因果证据；不能通过加大输入或重复试验解决 |
| 旧高层复杂速度 UDP | `320/325/321` 包到达 motion host UDP 43893，但机器人不前进 | 网络和基本 UDP 编码不是主因；厂商文档说明该接口要求厂商定义的自主模式 |

旧复杂速度包的含义为：

```text
320 / 0x0140: 前后速度，m/s
325 / 0x0145: 横移速度，m/s
321 / 0x0141: 转向速度，rad/s
```

厂商通信指南指出，这组复杂浮点速度指令用于其定义的**自主模式**。当前 firmware 状态
下，即使这些包到达 host，也没有使机器人行走。因此，最有证据支持的解释是：

> 旧接口的报文格式正确且能到达，但它不适用于当时的控制模式/接口组合。

这不是“电机故障”“站立控制器失效”或“UDP 无法到达”的证据，也不是要求通过 AI Motion
模式解锁来解决平地行走问题。

## 实际采取的解决步骤

我们阅读了厂商通信指南和厂商提供的参考控制脚本。参考脚本使用的是**简单轴指令**
而不是复杂速度指令：本地绑定端口 `20001`，定期发送心跳，按下前进时每 50 ms 发送
前进轴，释放时发送零轴。

在每个会使腿部动作的步骤前均获得现场即时授权，并按以下顺序执行：

```text
1. 手动控制模式：0x21010C02
2. 移动模式：    0x21010D06
3. 心跳：         0x21040001, value=0
4. 前进轴：       0x21010130, value=+32767
5. 停止：         0x21010130, value=0
```

所有上述包都是 little-endian 的 12 字节简单命令，并发送到 motion host UDP `43893`。
发送端遵循厂商参考脚本的源端口 `20001`。

### 分阶段安全验证

| 阶段 | 动作 | 结果 |
| --- | --- | --- |
| 模式切换 | 仅发送手动和移动模式命令 | 严格 host 抓包确认命令到达；不含速度或轴值 |
| 零轴健康检查 | 仅发送心跳和 `0x21010130 = 0` | host 确认到达；10 秒被动状态流约 200.1 Hz，2,000/2,000 `error_state=0` |
| 一次前进脉冲 | `+32767` 前进轴，名义 20 Hz，最长 1 秒 | 机器人前进后稳定停止；不自动重复 |
| 停稳检查 | 仅被动接收状态 10 秒 | 无前进命令、`motion_state=0`、2,000/2,000 `error_state=0` |

## 实体试验的可量化证据

在唯一的前进脉冲中：

| 指标 | 结果 |
| --- | ---: |
| 被动 `RobotState` 样本数 | 3,999 |
| `error_state=0` 样本 | 3,999 / 3,999 |
| 观测到的非零 `goal_vel_forward` 持续时间 | 1.110710 s |
| firmware `goal_vel_forward` 最大值 | 0.5012 |
| 测得 `vel_body[0]` 峰值 | 0.728896 m/s |
| 世界平面位移 | 0.401080 m |
| 现场观察 | 前进后稳定停止 |

host 严格抓包确认了来自 `192.168.1.103:20001` 的初始心跳、19 个 `+32767` 前进轴包和
零轴清理起点。固定包数抓包没有覆盖全部清零窗口；同步遥测也在 handle-state 反映零轴
前结束。因此，证据证明发送器开始清零、并且后续被动状态确认机器人已停止，但**不**声称
已经测得 firmware 的精确停止延迟。

完整原始证据和 SHA-256 索引见：

- [实体试验证据报告](/Users/timtan01/.copilot/session-state/7075f7df-a079-4744-bf8c-b4613c33fb96/files/lite3_manual_moving_forward_axis_20260824T083500Z/EVIDENCE.md)
- [host UDP 抓包](/Users/timtan01/.copilot/session-state/7075f7df-a079-4744-bf8c-b4613c33fb96/files/lite3_manual_moving_forward_axis_20260824T083500Z/host_udp_capture.txt)
- [SHA-256 索引](/Users/timtan01/.copilot/session-state/7075f7df-a079-4744-bf8c-b4613c33fb96/files/lite3_manual_moving_forward_axis_20260824T083500Z/SHA256SUMS)

## “自主模式”与“手动模式”的正确区分

### 两个不同层次

“自主”在本项目和厂商 firmware 中不是同一个概念：

| 层次 | 问题 | 示例 |
| --- | --- | --- |
| 项目层：软件自主决策 | 谁根据传感器、地图、目标和安全规则决定运动意图？ | 感知/规划/MAPPO 自动决定前进、减速、停止或转向 |
| 厂商 firmware 层：控制源模式 | firmware 按哪一类高层输入协议解释命令？ | 厂商定义的手动模式、自主模式、移动模式、原地模式 |

因此：

> 项目可以实现“软件自主决策”，同时在厂商称为“手动模式”的输入通道中发送外部程序
> 生成的轴指令。

这里的“手动模式”应理解为：firmware 接受类似遥控器摇杆的高层轴输入。这个轴输入可以
来自人手，也可以由外部程序持续发送；厂商标签本身不能说明谁在上层做决策。

### 控制状态不要混为一谈

| 名称 | 作用 | 当前证据 |
| --- | --- | --- |
| 手动 / 自主模式 | 厂商的控制源或协议模式 | 本次已验证手动模式下的外部简单轴输入 |
| 移动 / 原地模式 | 是否允许移动步态 | 本次已验证移动模式 |
| AI Motion Mode | 厂商特定 AI 运动/场景能力 | 不应默认等于自主模式；平地轴控制成功不依赖它 |

本项目不应从未文档化的 `RobotState` 数字字段推导这些状态的语义；模式是否接受必须通过
厂商文档、严格包证据和可测的机器人行为共同确认。

## 当前已验证的系统架构

```text
项目感知 / 规划 / MAPPO
        |
        | 高层运动意图（尚未接入实体）
        v
外部程序的安全约束与轴命令映射
        |
        | 心跳 + 简单轴指令
        v
Lite3 手动控制模式 + 移动模式
        |
        v
厂商板载平衡、步态、关节控制和保护
```

这条架构保留厂商板载的平衡和步态控制器。项目不需要自行生成 12 个关节目标，也不应使用
低层 MotionSDK 抢占控制权来替代厂商步态控制器。

当前受限工具在：

- [`lite3_control_mode_udp.py`](/Users/timtan01/workspace/mappo-arm-cloud-physical-ai.worktrees/setup-dependencies-deeprobotics-litex3/robot-stack/deep_robotics/lite3/locomotion/lite3_control_mode_udp.py)
- [`lite3_axis_udp.py`](/Users/timtan01/workspace/mappo-arm-cloud-physical-ai.worktrees/setup-dependencies-deeprobotics-litex3/robot-stack/deep_robotics/lite3/locomotion/lite3_axis_udp.py)

它们默认 dry-run；live 使用必须显式确认操作员已就绪。轴发送器当前仅允许零值或厂商参考
脚本使用的前进满量程值 `+32767`，并带有零轴清理、发送失败后的继续清零尝试和 socket
关闭逻辑。

## 这是否已经满足 repo 所需的“自动控制”模式？

### 可以确认的部分

**可以。** 从架构角度，当前已验证的手动模式轴接口可以作为外部自动控制程序的底层执行
通道：软件可以自动生成“虚拟摇杆”轴值，厂商板载控制器负责执行稳定行走。

### 不能确认的部分

**还不可以直接将完整导航系统接入并实体运行。** 当前尚未完成：

1. `+32767` 轴值到稳定 m/s 的厂商定义标定；
2. 小幅前进、停止、横移和转向的连续映射；
3. 外部程序、遥控器、急停和厂商 App 的输入优先级/抢占行为；
4. 轴指令丢失、网络抖动和 watchdog 超时的完整行为验证；
5. 视觉导航/MAPPO 的实体安全门禁、碰撞保护和授权流程；
6. 电机温度监测。当前高层 telemetry 不提供温度，因此不能进行连续或重复运行。

因此，当前合理的状态是：

| 结论 | 状态 |
| --- | --- |
| 厂商高层外部运动通道 | 已由一次受限试验证实 |
| 作为“虚拟摇杆”执行层的可行性 | 有正向证据，但需厂商确认持续自动输入契约 |
| 完整视觉导航/MAPPO 实体运行 | 未授权、未验证 |
| 重复或扩大实体运动试验 | 不允许，除非另行授权且补足热监控等门禁 |

## 研发团队需要对齐的决策

建议将当前问题明确为以下决策，而不是仅讨论“是否开启自主模式”：

### 决策 A：是否接受“软件自主 + 厂商手动轴接口”架构

推荐当前接受为**候选执行架构**，原因是它已经在目标 firmware 上产生了可量化的前进与停止
证据，并保留厂商步态/平衡控制器。

接受它不等于允许直接上线自动导航；它只决定后续可围绕该通道做离线适配和厂商协议确认。

### 决策 B：是否继续追求厂商“自主模式 + 320/325/321”接口

建议作为**并行的厂商澄清项**保留，而不是当前交付阻塞项。该接口在本机当前状态下曾达到
host 但没有使机器人移动；除非厂商提供本 firmware 的准确切换前置条件和可观察状态契约，
不应再次发送非零复杂速度命令。

### 决策 C：后续接入前的最低厂商确认

请向厂商确认：

1. 外部程序是否被正式允许在“手动 + 移动模式”下持续发送
   `0x21010130/131/135`，作为无人值守或半自主控制输入；
2. 三个轴指令的完整数值范围、归一化关系、方向约定、速度/加速度上限；
3. 心跳频率、轴更新频率、250 ms timeout 和零轴停止的确切语义；
4. 遥控器、App、外部 UDP 客户端和急停之间的优先级、接管和恢复规则；
5. firmware 可观察的控制模式状态字段或确认 ACK；
6. 官方电机温度/热保护 telemetry 接口；
7. 本 firmware 上“自主模式 + 320/325/321”是否支持外部程序控制，以及所需前置条件。



## 相关实现与状态记录

- [Lite3 平台说明](/Users/timtan01/workspace/mappo-arm-cloud-physical-ai.worktrees/setup-dependencies-deeprobotics-litex3/robot-stack/deep_robotics/lite3/README.md)
- [根项目状态表](/Users/timtan01/workspace/mappo-arm-cloud-physical-ai.worktrees/setup-dependencies-deeprobotics-litex3/README.md)
- [当前实现 diff](/Users/timtan01/workspace/mappo-arm-cloud-physical-ai.worktrees/setup-dependencies-deeprobotics-litex3/lite3-vendor-axis-workaround-20260824.diff)
