# depth_camera_sight — reference media (slot)

Captured **on the robot** (see [`../README.md`](../README.md)). Expected files — the descriptor's
`front_camera` / `vio_depth` `calibration.reference_media` point here:

| File | What it is |
|---|---|
| `rgb_sample.jpg` | A front-camera RGB frame (undistorted). |
| `depth_sample.jpg` | A RealSense depth frame (colourized). |
| `scene_closeup.jpg` | A close-up of the manipulation scene. |

**PENDING an on-robot capture** — the descriptor keeps the camera poses `PENDING` until these exist.
